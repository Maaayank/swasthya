<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowDisablePreview">true</item>
        <item name="colorPrimary">@color/color3</item>
        <item name="colorPrimaryDark">@color/color1</item>
        <item name="colorAccent">@color/color3</item>
        <item name="android:fontFamily">serif-monospace</item>
    </style>

    <style name="MyToolbarTheme"
        parent="@style/AppTheme">
        <item name="colorControlNormal">@color/color2</item>
        <item name="titleTextColor">@color/color2</item>
    </style>

</resources>
